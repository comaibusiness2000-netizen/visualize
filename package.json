{
  "name": "visualize-ios-app",
  "version": "0.1.0",
  "private": true,
  "main": "node_modules/expo/AppEntry.js",
  "scripts": {
    "start": "npx expo start --tunnel",
    "start:lan": "npx expo start --lan",
    "doctor": "npx expo-doctor",
    "ios:build": "npx eas-cli build --platform ios",
    "ios:submit": "npx eas-cli submit --platform ios",
    "web:preview": "node tools/serve-preview.mjs"
  },
  "dependencies": {
    "expo": "~55.0.0",
    "expo-image-picker": "~55.0.20",
    "expo-speech": "~55.0.14",
    "react": "^19.2.0",
    "react-native": "^0.83.6"
  },
  "devDependencies": {
    "@babel/core": "^7.25.0",
    "@expo/ngrok": "^4.1.0"
  }
}
